mixin video(video)
    div
        h4=video.title
        ul
            li #{video.rating}/5
            li #{video.comments} comments.
            li Posted 
                b #{video.createdAt}
            li #{video.views} views.